#include<stdio.h>
int fact(int n)
{
	if(n==0)
	return 1;
	else
	return n*fact(n-1);
}
int main()
{
	int f;
	float n;
	printf("enter the value:");
	scanf("%f",&n);
	if(n<=0||int(n)!=n)
	{
		printf("invalid");
	}
	else
	{
		f=fact(n);
		printf("\nfactorial of % is %d",f);
	}
	return 0;
}
